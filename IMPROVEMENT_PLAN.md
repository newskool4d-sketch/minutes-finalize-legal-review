# 회의록 정본화·법률검토 스킬 개선계획

- 작성일: 2026-07-25
- 대상: `minutes-hwpx-finalize-legal-review`
- 기준 저장소: `minutes-finalize-legal-review`
- 목적: 1차 수정 회의록에서 결재용 실명 정본 HWPX와 정보공개 청구용 비실명 HWPX를 형식에 맞게 제출하고, 법률위험 검토표를 함께 생성하는 범용 스킬로 발전시키기 위한 후속 작업 정의

## 0. 구현 점검 (2026-07-26)

v0.4.1에서 계획의 P0·P1·배포 기반 P2 항목을 구현·검증했다.

| 구분 | 구현·검증 근거 | 상태 |
|---|---|---|
| P0 안전 경계·법률위험·법령 시점·MCP 통제 | 이중 산출물, 법률위험 스키마, `legal-research-protocol.md`, 비식별 MCP 규칙 | 완료 |
| P1 HWPX·합성 평가·설치 | 승인 반영·교차검수·레이아웃 캐시 제거, 13개 합성 회귀, Codex·Claude 격리 설치 검증 | 완료 |
| P2 프로필·CI·라이선스·운영지표 | 2개 프로필, GitHub Actions, MIT, 비식별 운영지표 검증·3건 이상 집계기, `v0.4.1` 태그 | 완료 |

실제 운영에서만 가능한 다음 단계는 남아 있다.

- 서로 다른 3~5건에 대해 운영지표를 축적하고, 반려 사유·누락·검토시간을 재평가한다.
- 각 실제 건의 사건일·심의일·처분일, 당시 시행 법령, 법무·정보공개 담당 판단을 입력해 최종 정본을 확정한다.

이는 코드 미구현이 아니라 현업 자료와 담당자 판단이 필요한 시범 운영 단계다. 실제 사건자료·실명·HWPX는 공개 저장소에 넣지 않는다.

## 1. 현재 판단

현재 버전은 업무 목적, 기본 검토 절차, 위험 발언 유형, 사람 승인 원칙, 법령 MCP 우선 사용 원칙을 갖춘 **명세 초안**이다. 실제 현업 적용을 위해서는 다음 세 축을 보강해야 한다.

1. 결재용 실명 정본과 정보공개 청구용 비실명본을 안전하게 읽고 다시 HWPX로 만드는 결정론적 처리 도구
2. “법률적으로 문제될 수 있는 발언”을 일관되게 분류하는 판정 기준과 근거 기록 구조
3. 익명·합성 자료를 이용한 회귀시험 및 설치·배포 자동화

현재 상태에서 실제 사건 파일에 바로 일괄 적용하기보다는, 정본 수정 제안과 법률위험 표시를 생성하고 담당자가 승인하는 보조 도구로 제한하는 것이 타당하다.

## 2. 우선순위 요약

| 우선순위 | 작업 | 현재 문제 | 완료 기준 |
|---|---|---|---|
| P0 | 설치 경로명 통일 | README의 설치 폴더명과 `SKILL.md`의 스킬명이 다름 | Codex·Claude 설치 경로와 스킬명이 `minutes-hwpx-finalize-legal-review`로 일치 |
| P0 | 이중 HWPX 산출물 고정 | 공개용 비식별본의 목적·형식·대조 기준이 불명확 | 결재용 실명 정본과 정보공개 청구용 비실명 HWPX가 모두 생성되고 교차검수됨 |
| P0 | 기록 보존 원칙 강화 | 문제가 있는 발언을 “삭제”하는 것으로 오해할 수 있음 | 원 발언 보존, 정본 수정, 정보공개 청구용 비실명 처리가 명확히 분리됨 |
| P0 | 법률위험 판정표 구체화 | `높음·중간·낮음`의 경계와 법적 쟁점 유형이 불명확 | 동일 문장에 대해 검토자 간 판정 편차를 줄이는 기준표 마련 |
| P0 | 법적 근거의 시점 관리 | 현행법과 사건 당시 적용법 구분이 약함 | 사건일·심의일·처분일별 적용 법령과 시행일 기록 |
| P0 | 민감정보·MCP 전송 통제 | 원격 MCP URL과 질의에 사건정보가 노출될 수 있음 | 로컬 우선, 최소 질의, 비식별화, 키 비노출 검증 절차 마련 |
| P1 | HWPX 처리 스크립트 | 현재는 실제 HWPX 수정·보존을 보장하는 도구가 없음 | 추출→식별→제안 반영→패키지 검증을 자동 수행 |
| P1 | 출력 스키마 확장 | 법령명·조문·시행일·조회일·출처가 행 스키마에 없음 | 모든 법률 판단 행에서 근거와 확인 상태를 구조화 |
| P1 | 합성 테스트 세트 | 실제 자료를 저장소에 넣을 수 없어 회귀시험이 없음 | 비식별 합성 사례와 기대 결과로 자동 평가 가능 |
| P1 | 설치·검증 자동화 | 사용자가 여러 폴더를 수동 복사해야 함 | PowerShell 설치·검증 스크립트와 제거 전 확인 절차 제공 |
| P2 | 다기관 프로필 | 현재 교육활동 침해 업무 프로필 1개만 존재 | 기관·위원회별 프로필 추가가 공통 명세 변경 없이 가능 |
| P2 | CI·릴리스 체계 | 로컬 검증과 GitHub 배포가 수동임 | 푸시 시 구조·링크·비밀·샘플 검증, 버전 태그 제공 |
| P2 | 운영 성과지표 | 처리시간·오탐·누락 개선 여부를 측정하지 않음 | 사람 검토시간, 수용률, 치명적 누락률을 기록 |

## 3. P0: 실제 사용 전에 먼저 개선할 항목

### 3.1 설치 경로와 패키지 이름 통일

현재 `SKILL.md`의 이름은 `minutes-hwpx-finalize-legal-review`지만 README의 Codex·Claude 설치 예시는 `minutes-finalize-legal-review` 폴더로 복사한다. 스킬 생성 규칙상 폴더명은 스킬명과 같게 유지하는 편이 안전하다.

개선 작업:

- README의 `$target`을 모두 `minutes-hwpx-finalize-legal-review`로 변경한다.
- 저장소 이름은 유지할 수 있지만, 설치 대상 폴더명과 스킬명은 일치시킨다.
- 설치 후 `SKILL.md` 존재, frontmatter 이름, 참조 파일 존재 여부를 검사한다.
- Codex와 Claude Code의 실제 최신 스킬 검색 경로는 각 공식 문서에서 다시 검증한다.

완료 조건:

- 새 PC에서 README만 따라 설치했을 때 스킬이 한 번에 검색된다.
- 설치된 폴더 안에 `SKILL.md`, `references/`, `profiles/`, 필요한 에이전트 메타데이터가 존재한다.

### 3.2 “삭제”와 기록 보존의 경계 명확화

법률적으로 문제가 될 수 있는 발언이 있다는 사실과 그 발언을 회의록에서 제거할 수 있다는 결론은 동일하지 않다. 행정심판·행정소송 대비 목적이라면 오히려 어떤 발언이 있었고 어떤 이유로 정리되었는지 추적 가능해야 한다.

산출물을 다음과 같이 분리한다.

1. `입력보존본`: 1차 수정본을 변경 없이 보존하고 해시를 기록
2. `결재용_실명정본`: 오기·중복·표현을 정리하되 사실과 의사결정 과정 및 실명을 보존한 HWPX
3. `법률위험_검토표`: 문제 발언, 위험 이유, 근거, 권고 조치, 승인 상태 기록
4. `정보공개청구용_비실명본`: 결재용 실명 정본을 기준으로 승인된 식별정보만 처리한 HWPX
5. `변경이력`: 원 위치, 변경 전후 요지, 변경 사유, 승인자 기록

`D 삭제 검토`는 자동 삭제 명령이 아니라 다음 중 하나를 사람이 결정하는 상태로 재정의한다.

- 원문 유지
- 출처를 붙여 중립화
- 발언 요지로 축약
- 결재용에는 유지하고 정보공개 청구용에서만 비실명 처리
- 법무 확인 후 정정 또는 제외

완료 조건:

- AI가 원 발언을 흔적 없이 삭제하는 경로가 없다.
- 결재용 실명 정본 HWPX와 정보공개 청구용 비실명 HWPX가 파일명·목적·승인 절차상 분리된다.
- 두 HWPX의 의결·표결·사실관계·표와 문단 구조가 일치하고, 비실명처리 대장이 존재한다.

### 3.3 법률위험 판정체계 확장

현재 체크리스트는 낙인·추측·민감정보 탐지에는 유용하지만, 행정심판·행정소송에서 문제될 수 있는 쟁점 전체를 충분히 구조화하지 못한다.

추가할 위험 유형:

- `절차`: 의견진술 기회, 통지, 제척·기피·회피, 의사정족수, 표결, 이유 제시
- `사실인정`: 출처 없는 단정, 전언 의존, 반대 증거 누락, 진술 왜곡
- `재량판단`: 무관한 요소 고려, 관련 요소 누락, 비례·형평 문제, 점수와 조치 불일치
- `편견·예단`: 인격·가정환경·외모·평판을 근거로 한 판단, 심의 전 결론 암시
- `개인정보`: 목적 외 개인정보, 민감정보, 제3자 정보, 과도한 식별정보
- `기록무결성`: 발언자 오인, 표결 수치 불일치, 사후 수정 근거 없음
- `명예·분쟁확대`: 확인되지 않은 범죄·질환·의도·평판을 사실처럼 기록
- `법적 근거`: 폐지·개정 전 조문, 잘못된 조문, 사건 당시 미시행 기준 적용

위험도 기준 초안:

| 수준 | 판정 기준 | 기본 조치 |
|---|---|---|
| 높음 | 처분의 절차적 적법성·사실인정·중대한 조치에 직접 영향을 줄 수 있거나 민감정보 침해가 큼 | `L 법무 확인`, 정본 확정 중지 |
| 중간 | 판단 편향 또는 분쟁 확대 가능성이 있으나 다른 자료로 보완 가능 | `N 중립화` 또는 `V 근거 확인`, 담당자 승인 |
| 낮음 | 법률효과보다 표현·가독성·경미한 개인정보 최소화 문제에 가까움 | `S 요지화` 또는 `K 유지` |

판정 시 반드시 `위험 문구 → 법적 쟁점 → 사실관계 → 적용 근거 → 권고 조치`의 연결을 남긴다. 단순히 불편하거나 부적절한 표현이라는 이유만으로 법률위험으로 분류하지 않는다.

### 3.4 적용 법령의 시간축 관리

법령 검토는 “현재 법령”만 조회해서는 부족하다. 최소한 다음 날짜를 입력받도록 한다.

- 사건 발생일
- 심의일
- 조치 요청일 또는 처분일
- 회의록 정본 작성일

법령 근거 레코드에 다음 필드를 추가한다.

```json
{
  "jurisdiction": "대한민국",
  "law_name": "법령명",
  "article": "조문",
  "promulgation_date": "YYYY-MM-DD",
  "effective_date": "YYYY-MM-DD",
  "applicable_event_date": "YYYY-MM-DD",
  "retrieved_at": "YYYY-MM-DD",
  "source_type": "법령|고시|판례|재결례|내부규정",
  "source_id": "공식 식별자 또는 URL",
  "verification_status": "원문확인|요약만확인|법무확인필요"
}
```

완료 조건:

- 현행 조문과 사건 당시 적용 조문이 다르면 두 버전을 구분해 표시한다.
- 시행일이 확인되지 않은 근거로 위법 여부를 확정하지 않는다.
- `references/legal-basis.md`와 프로필에는 최종 확인일과 다음 재검토 시점을 둔다.

### 3.5 MCP 및 개인정보 안전장치

Korean Law MCP를 포함한 외부 도구는 법률 근거 조회용으로만 사용하고, 회의록 전문을 보내는 통로로 사용하지 않는다.

필수 통제:

- 로컬 MCP 또는 기관 승인 커넥터를 우선한다.
- 원격 MCP에는 사건번호·실명·학교명·연락처·건강정보를 제거한 최소 검색어만 전송한다.
- OC 키를 URL 질의문자열로 사용하는 방식은 브라우저 기록·프록시 로그에 남을 수 있으므로 민감 환경에서는 환경변수 또는 승인된 비밀 저장소를 우선한다.
- MCP 결과는 신뢰 경계 밖의 자료로 취급하고, 조문 원문·시행일·공식 출처를 다시 확인한다.
- 로그와 검수요약에는 실제 API 키, 전체 원문, 직접 식별정보를 남기지 않는다.
- MCP 장애 시 공식 국가법령정보센터 원문과 기관 내부 기준으로 전환한다.

완료 조건:

- 저장소, Git 이력, 로그, 산출물에서 실제 키가 발견되지 않는다.
- 원격 전송 전 비식별화 확인 단계가 있다.
- MCP 미설치 상태에서도 `V 근거 확인`과 `L 법무 확인`을 이용해 안전하게 종료할 수 있다.

## 4. P1: 기능 신뢰성을 위한 구현

### 4.1 결정론적 HWPX 처리 도구

현재 스킬은 HWPX 산출을 요구하지만, 패키지 구조와 서식을 보존하는 실행 도구가 포함되어 있지 않다. 반복 업무에 사용하려면 다음 스크립트 또는 동등한 도구가 필요하다.

제안 구조:

```text
scripts/
  inspect_hwpx.py
  extract_minutes_model.py
  apply_approved_edits.py
  validate_hwpx.py
  scan_sensitive_content.py
```

기능 요구:

- HWPX ZIP 구조와 XML 유효성 검사
- 문단·표·셀·발언자·쪽 단서를 안정적인 위치 식별자로 변환
- 원본 서식, 표 구조, 머리말·꼬리말, 각주, 개체를 가능한 범위에서 보존
- 승인된 수정만 반영
- 반영 전후 파일 해시와 변경 목록 생성
- 결과 파일 재개방 검사 및 HWPX 패키지 무결성 검사
- 원본 파일 덮어쓰기 차단

위치 식별자는 단순 쪽 번호만 사용하지 말고 다음과 같이 구성한다.

```text
section-1/paragraph-42
section-1/table-3/row-5/cell-2/paragraph-1
```

페이지 번호는 편집 후 달라질 수 있으므로 보조 정보로만 기록한다.

### 4.2 출력 스키마 확장

`references/output-schema.md`에 다음 필드를 추가한다.

- `issue_id`: 변경되지 않는 항목 ID
- `location_id`: HWPX 구조 기반 위치
- `statement_text_hash`: 원문 전체를 노출하지 않는 동일성 확인값
- `legal_issue_type`: 절차·사실인정·재량·편견·개인정보·기록무결성·법적근거
- `legal_basis`: 법령 근거 레코드 배열
- `reasoning_summary`: 근거와 사실관계의 짧은 연결 설명
- `counterevidence`: 반대 자료 또는 유리한 진술
- `recommended_scope`: 결재용 실명 정본·정보공개 청구용 비실명본·양쪽
- `confidence`: 모델 확신이 아니라 자료 충족도
- `human_decision`: 유지·수정·가림·반려·법무확인
- `decided_by_role`, `decided_at`

`confidence`는 다음처럼 자료 충족도로 정의한다.

- `충분`: 공식 자료와 반대 자료를 모두 대조
- `부분`: 일부 자료만 확인
- `부족`: 전언·추측 또는 핵심 자료 없음

### 4.3 승인 상태와 역할 분리

권장 상태 흐름:

```text
AI 제안
→ 주무관 1차 검토
→ 담당 장학사 판단
→ 필요 시 법무 확인
→ 승인된 변경만 반영
→ 정본·검토표 교차검수
→ 결재
```

역할별 권한:

- AI: 탐지·분류·수정안 제안
- 주무관: 오기·시나리오·기초자료 대조
- 담당 장학사: 맥락·필요성·정본 반영 판단
- 법무 담당: 중대한 절차·처분·개인정보 쟁점 확인
- 최종 결재자: 정본 승인

한 사람이 여러 역할을 맡을 수 있지만, 산출물에는 어떤 역할로 판단했는지를 기록한다.

### 4.4 합성·비식별 평가자료 구축

실제 회의록은 저장소에 넣지 않는다. 대신 실제 업무 패턴을 재현한 합성 HWPX와 기대 결과를 만든다.

최소 평가 사례:

1. 단순 오탈자와 발언자 오기
2. 표결 수치와 본문 결론 불일치
3. 정신건강·장애를 근거 없이 추정한 발언
4. 학생·보호자에게 유리한 진술 누락
5. 과거 사건·평판을 현재 사안 판단에 사용
6. 고의성·지속성 점수 이유 혼동
7. 의견진술 기회 또는 불참 처리 기록 누락
8. 결재용 실명 정본 HWPX와 정보공개 청구용 비실명 HWPX의 차이
9. 사건 당시와 현재 법령 버전이 다른 사례
10. MCP 미설치·오류·불완전 응답 사례

평가 지표:

- 치명적 위험 누락률
- 과도한 삭제 제안률
- 사실·수치·표결 변형 건수
- 위험 유형 분류 일치율
- 법적 근거 완전성
- HWPX 구조·서식 보존 여부
- 사람이 수용한 수정안 비율
- 사건 1건당 검토시간 절감

법률 고위험 사례는 단순 정확도보다 **치명적 누락 0건**과 **근거 없는 단정 0건**을 우선한다.

### 4.5 설치 및 검증 자동화

추가 후보:

```text
scripts/install.ps1
scripts/uninstall.ps1
scripts/verify_install.ps1
```

설치 스크립트 요구:

- 대상 경로를 절대경로로 확인
- 기존 설치가 있으면 덮어쓰기 전에 차이와 백업 경로 제시
- Codex 또는 Claude 대상 선택
- 설치 후 frontmatter·참조 파일·버전 확인
- API 키를 저장하거나 출력하지 않음

제거 스크립트는 설치한 정확한 폴더만 대상으로 하고, 삭제 전 경로와 파일 목록을 표시해야 한다.

## 5. P2: 범용 배포와 운영 품질

### 5.1 프로필 확장

공통 `SKILL.md`에는 보편 절차만 유지하고, 다음을 프로필로 분리한다.

- 지역교권보호위원회
- 학교교권보호위원회 또는 과거 제도 자료 검토
- 다른 행정위원회·심의위원회
- 기관별 회의록 기재 기준
- 정보공개 청구용 비실명 처리 기준

각 프로필은 관할, 기준일, 적용 법령, 필수 절차, 점수·조치 기준, 내부 확인 항목, 공식 출처를 포함한다.

### 5.2 공개 배포 요건

범용 배포 전 확인할 사항:

- 사용·수정·재배포 권한을 명확히 하는 라이선스 선택
- 지원 버전과 최소 환경 명시
- Codex·Claude 설치법을 각각 실제 환경에서 검증
- 릴리스 버전과 법령 프로필 버전을 분리 표기
- 보안 취약점·법령 오류 제보 경로 마련
- 실제 사건자료를 받지 않는 이슈 템플릿 제공

현재 저장소에 라이선스가 없다면 다른 사용자는 법적으로 자유로운 재사용 권한을 명확히 확인하기 어렵다. MIT, Apache-2.0 등 후보 중 운영 목적에 맞는 라이선스를 사용자가 선택해야 한다.

### 5.3 자동 검증

GitHub Actions 후보:

- `SKILL.md` frontmatter와 폴더명 검증
- `agents/openai.yaml` 일치 여부
- Markdown 링크 검사
- JSON 예시 파싱
- 합성 HWPX 검사
- 비밀·개인정보 패턴 검사
- 실제 `.hwp`, `.hwpx`, 원본·실명 파일 커밋 차단
- PowerShell 설치 스크립트 정적 검사

`.gitignore`만으로 민감자료 유출을 완전히 막을 수 없으므로, 커밋 전 스캐너와 CI 검사를 함께 둔다.

### 5.4 운영 피드백

각 심의 후 개인정보 없이 다음 수치만 축적한다.

- 전체 쪽 수와 검토 소요시간
- AI 제안 수, 승인 수, 반려 수
- 높은 위험 항목 수와 법무 확인 수
- 담당자가 새로 발견한 누락 수
- 표결·사안번호·날짜 오류 수
- HWPX 서식 훼손 여부

3~5건의 시범 적용 후 판정 기준과 프롬프트를 조정하고, 10건 이상에서 치명적 누락과 과도한 삭제 제안 추이를 확인한다.

## 6. 권장 구현 순서

### 1차 개선: 안전하게 설치하고 판단하기

1. 설치 폴더명 수정
2. 기록 보존·삭제 경계 개정
3. 법률위험 유형과 위험도 기준 추가
4. 출력 스키마에 법적 근거·승인 필드 추가
5. MCP 개인정보 안전장치 강화

### 2차 개선: 실제 HWPX를 안정적으로 처리하기

1. HWPX 검사·추출 스크립트
2. 승인 변경 반영 스크립트
3. 구조·서식·패키지 검증
4. 합성 HWPX 5건 이상 제작
5. 실제 한글 프로그램 열기 검증

### 3차 개선: 여러 사람이 설치하고 반복 사용하기

1. Codex·Claude 설치 자동화
2. CI와 비밀정보 검사
3. 라이선스·버전 정책
4. 추가 기관 프로필
5. 시범 운영 지표 분석

## 7. 다음 작업에서 필요한 결정

다음 항목은 구현 전에 사용자가 정해야 한다.

1. 결재용 실명 정본에서 발언을 어느 수준까지 요지화할 수 있는지에 대한 기관 내부 원칙
2. 법률 검토 결과를 HWPX 안에 넣을지 별도 검토표로만 관리할지
3. HWPX 자동 수정까지 할지, 초기 버전은 수정 제안표 생성에 한정할지
4. 공개 저장소의 라이선스
5. 합성 샘플을 실제 사례 패턴 기반으로 만들 수 있는지
6. 원격 MCP 사용이 허용되는 정보 범위
7. 최종 법무 확인 담당자와 승인 상태 명칭

권장 기본값은 **원본 보존 + 결재용 실명 정본 HWPX + 정보공개 청구용 비실명 HWPX + 별도 검토표 + 승인된 변경만 반영 + 로컬 MCP 우선**이다.

## 8. 최종 완료 정의

다음 조건을 모두 충족하면 현업 시범 적용 가능한 버전으로 본다.

- 설치 경로와 스킬명이 일치한다.
- 원본·결재용 실명 정본 HWPX·정보공개 청구용 비실명 HWPX·검토표가 분리된다.
- 두 HWPX의 의결·표결·사실관계·표와 문단 구조가 일치하며, 승인된 비실명 처리만 다르다.
- 승인 없이 삭제 또는 HWPX 변경이 발생하지 않는다.
- 사건 당시 적용 법령과 시행일이 기록된다.
- 모든 높은 위험 판정에 사실관계와 공식 근거가 연결된다.
- MCP 장애 시 안전한 대체 경로가 작동한다.
- 합성 사례에서 치명적 위험 누락과 사실·표결 변형이 없다.
- 생성 HWPX가 패키지 검사와 실제 한글 프로그램 열기 검증을 통과한다.
- Git 저장소와 로그에 실제 사건자료·실명·API 키가 없다.
- 주무관과 담당 장학사의 검토시간 및 반려 사유를 기록할 수 있다.

이 문서는 개선 작업의 범위와 순서를 정하는 계획서다. 법령 내용과 기관별 회의록 수정 가능 범위는 실제 구현 시 법령 MCP·공식 원문·기관 내부 규정 및 필요 시 법무 담당자의 확인을 거쳐 확정한다.
